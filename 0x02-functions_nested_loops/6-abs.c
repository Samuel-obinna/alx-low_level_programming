#include"main.h"

/**
 * _abs - Compute absolute value of integers
 * Code written by Sazzy
 * @n: Integer to be computed
 * Return: The absolute value of integers
*/

int _abs(int n)
{
	if (n >= 0)
		return (n);

	else
		return (-n);
}
